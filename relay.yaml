project: sandbox

containers:
  - name: app
    dockerfile: app/Dockerfile

services:
  - name: app
    container: app
    ports:
      - name: http
        port: 3000

deploys:
  - name: dev
    services:
      - service: app
        host: sandbox.hostbo.cx
        port: 80
        target: http
