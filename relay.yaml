project: sandbox

containers:
  - name: app
    dockerfile: app/Dockerfile
  
  - name: db
    image: postgres:15-alpine
    env:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: user
      POSTGRES_DB: toy_db

services:
  - name: app
    container: app
    ports:
      - name: http
        port: 3000
    env:
      DATABASE_URL: postgres://user:password@172.17.0.1:9001/toy_db
  
  - name: db
    container: db
    env:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: user
      POSTGRES_DB: toy_db
    ports:
      - name: pg
        port: 5432

deploys:
  - name: dev
    services:
      - service: app
        host: sandbox.hostbo.cx
        port: 443
        target: http
      - service: db
        host: sandbox-db.hostbo.cx
        port: 5434
        target: pg
